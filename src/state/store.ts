import { defineStore } from "pinia";

export const useStore = defineStore("lectureList", {
  // DATA -> ref()
  state: () => ({}),

  // COMPUTED -> computed()
  getters: {},

  // METHODS -> function()
  actions: {},
});
